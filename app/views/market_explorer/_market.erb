<div class="row">
  <div class="box">
    <div class="box-body">
      <table id="lists" class="table table-bordered table-striped" cellspacing="0" width="100%">
        <thead>
        <tr>
          <th>Count</th>
          <th>price</th>
          <th>location</th>
          <th>issued</th>
          <th>duration</th>
        </tr>
        </thead>

        <tbody>
        <% if @markets.present? %>
            <% @markets.market_details.each do |market| %>
                <tr>
                  <td><%= market.volume %></td>
                  <td><%= number_to_currency(market.price,
                                             :precision => 2,
                                             :unit => "isk",
                                             :separator => ".",
                                             :format => "%n %u") %>
                  </td>
                  <td><%= market.station_id %></td>
                  <td><%= market.issued %></td>
                  <td><%= market.duration %></td>
                </tr>
            <% end %>
        <% end %>
        </tbody>
      </table>
    </div>
  </div>
</div>
<br>
